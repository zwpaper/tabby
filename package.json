{
  "name": "ragdoll",
  "private": true,
  "scripts": {
    "check": "npm-run-all check:default check:test",
    "check:default": "biome check --config-path=./biome.default.json --write .",
    "check:test": "biome check --config-path=./biome.test.json --write .",
    "test": "vitest --run",
    "tsc": "bun --filter '*' tsc",
    "build": "bun --filter '*' build",
    "prepare": "husky"
  },
  "workspaces": ["packages/*"],
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/bun": "^1.2.6",
    "@vitest/coverage-v8": "^3.0.9",
    "husky": "^9.1.7",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.7.2",
    "vitest": "^3.0.9"
  }
}
