check_git_clean() {
    git diff --exit-code
    git diff --staged --exit-code
}

main() {
    bun fix
    check_git_clean
    bun tsc
    bun dep-check
    bun run test 
    cd packages/vscode && bun run test
}

main
