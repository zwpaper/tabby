check_git_clean() {
    git diff --exit-code
    git diff --staged --exit-code
}

main() {
    bun dep-check
    bun fix
    check_git_clean
    bun tsc
    bun install --frozen-lockfile
    bun run test 
    cd packages/vscode && bun run test
}

main
