check_git_clean() {
    git diff --exit-code
    git diff --staged --exit-code
}

main() {
    check_git_clean
    bun run check
    bun --filter "*" tsc
    bun run test 
    check_git_clean
}

main
