id: no-console
language: tsx
rule:
  pattern: console.$$$
message: Don't use console for logging, use getLogger instead.
severity: error
files:
  - "private/packages/runner/src/**/*.ts"
  - "private/packages/server/src/**/*.ts"
